{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build:next": {
      "dependsOn": ["build:registry"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "build:registry": {
      "outputs": ["public/r/**"]
    }
  }
}
